package com.focuslock.ui.design

import androidx.compose.ui.graphics.Color

/**
 * "Quiet Discipline" palette — warm charcoal surfaces, restrained amber/cream
 * accents, reserved red for destructive/emergency actions. Dark-only by design.
 */
object FocusColors {

    // Charcoal surfaces
    val Charcoal900 = Color(0xFF0E1116)
    val Charcoal800 = Color(0xFF161B22)
    val Charcoal700 = Color(0xFF1B2129)
    val Charcoal600 = Color(0xFF222933)
    val Charcoal500 = Color(0xFF2A313B)

    // Ivory text
    val Ivory = Color(0xFFEDE6DA)
    val IvoryDim = Color(0xFFA8A29A)
    val IvoryFaint = Color(0xFF7A7570)

    // Amber commitment accent
    val Amber = Color(0xFFD9A441)
    val AmberLight = Color(0xFFF0DC9E)
    val AmberDark = Color(0xFF8A6D2F)
    val OnAmber = Color(0xFF1A1408)

    // Reserved red (destructive / emergency only)
    val Red = Color(0xFFC4574E)
    val RedDark = Color(0xFF3A201E)
    val OnRed = Color(0xFF1A0D0C)

    // Green (status success)
    val Green = Color(0xFF6FBF8B)
    val GreenDark = Color(0xFF1E2F24)

    // Outlines
    val Outline = Color(0xFF3D4550)
    val OutlineFaint = Color(0xFF2A313B)
}
